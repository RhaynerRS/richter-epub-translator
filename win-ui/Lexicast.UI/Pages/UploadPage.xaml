<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Lexicast.UI.Pages.UploadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lexicast.UI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="40,32" Spacing="20" MaxWidth="640" HorizontalAlignment="Center">

            <StackPanel Spacing="4">
                <TextBlock Text="Lexicast EPUB Translator" Style="{StaticResource TitleLargeTextBlockStyle}" />
                <TextBlock Text="Envie um arquivo .epub para traduzir usando a API."
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <!-- File selection -->
            <StackPanel Spacing="8">
                <TextBlock Text="Arquivo EPUB" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0"
                            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="4" Padding="12,8">
                        <TextBlock x:Name="SelectedFileText"
                                   Text="Nenhum arquivo selecionado"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center" />
                    </Border>
                    <Button Grid.Column="1" Content="Selecionar..." Click="PickFileButton_Click" />
                </Grid>
            </StackPanel>

            <!-- Target language -->
            <StackPanel Spacing="8">
                <TextBlock Text="Idioma de destino" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBox x:Name="TargetLanguageBox" PlaceholderText="ex: pt-BR, en, es" Text="pt-BR" />
            </StackPanel>

            <!-- Concurrency -->
            <StackPanel Spacing="8">
                <TextBlock Text="Concorrência" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <NumberBox x:Name="ConcurrencyBox"
                           Value="1" Minimum="1" Maximum="32"
                           SpinButtonPlacementMode="Inline"
                           SmallChange="1" />
            </StackPanel>

            <!-- Optional prompt -->
            <StackPanel Spacing="8">
                <TextBlock Text="Instruções adicionais (opcional)" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBox x:Name="UserPromptBox"
                         PlaceholderText="ex: mantenha os nomes próprios no idioma original"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         Height="80" />
            </StackPanel>

            <!-- API base URL -->
            <StackPanel Spacing="8">
                <TextBlock Text="URL da API" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBox x:Name="ApiUrlBox" Text="http://localhost:8000" />
            </StackPanel>

            <InfoBar x:Name="ErrorInfoBar"
                     Severity="Error"
                     IsOpen="False"
                     Title="Não foi possível iniciar a tradução" />

            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button x:Name="TranslateButton"
                        Content="Traduzir"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="TranslateButton_Click" />
                <ProgressRing x:Name="SubmitProgressRing" Width="24" Height="24" IsActive="False" />
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
